#include <stdio.h>
#include "dog.h"

/**
 * void print_dog - prints struct dog
 * @struct dog: to be printed
 * @d: input
 */

void print_dog(struct dog *d);
{
	if (d == NULL)
		printf(nil);
	else{
		d->name = name
			printf("%c", name);
		d->age = age
			printf("%f", age);
		d->owner = owner
			pintf("%c",owner);
}
