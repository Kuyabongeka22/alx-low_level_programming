#include <stdlib.h>
#include "lists.h"

/*size_t print_dlistint(const dlistint_t *h){
        dlistint_t *head;
        dlistint_t *nodeA;

        nodeA = malloc(sizeof(dlistint_t));
        nodeA->n = h;
        nodeA->prev = NULL;
        nodeA->next = head;
        *creating a head
        head = nodeA;
        *creating a temporal pointer
        dlistint_t *temp;
        temp = head;
        *transversing a linked list and printing ea member
        while(temp->next != NULL){
                printf(%d, temp->n);
        }
        return (sizeof(dlistint_t));*/
